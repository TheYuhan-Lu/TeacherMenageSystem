TeacherManagementSys folder is a Qt project folder,and you can open it by Qt6.1.1(maybe other version)
!!!!The most important thing is that you should copy the two txt file to the debug folder in the same Qt folder(you can see the path in the Qt ) after you build once(because there use a function that will read the exe path so you could also rewrite the part)
